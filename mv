The mv Command in Unix

The mv command has two uses; 
  * It can be used to move a file from a location to a set destination. 
  * It can also be used to rename a file by stating current file name followed by desired file name.

There are several options for the mv command
  -f will cause it to overwrite without prompt
  -i will prompt if move causes file to be overwritten
  -n do not overwrite an existing file
